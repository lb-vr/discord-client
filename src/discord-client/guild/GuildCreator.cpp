#include "discord-client/guild/GuildCreator.hpp"
/*
lbvr::guild::GuildCreator::GuildCreator(
	const string_d & name,
	const voice_region_d & region,
	const base64_d & icon,
	const verification_level_d & verification_level,
	const message_notification_level_d & default_message_notifications,
	const content_filter_level_d & explicit_content_filter,
	const roles_d & roles,
	const channels_d & channels)
{
	name_							= _mkbase<string_d>(name);
	region_							= _mkbase<voice_region_d>(region);
	icon_							= _mkbase<base64_d>(icon);
	verification_level_				= _mkbase<verification_level_d>(verification_level);
	default_message_notifications_	= _mkbase<message_notification_level_d>(default_message_notifications);
	explicit_content_filter_		= _mkbase<content_filter_level_d>(explicit_content_filter);
	roles_							= _mkbase<roles_d>(roles);
	channels_						= _mkbase<channels_d>(channels);
}

*/
